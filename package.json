{
  "name": "vpn4u-site",
  "version": "1.0.0",
  "private": true,
  "description": "VPN4U infrastructure storefront and merchant-verification website",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "NODE_ENV=development node --watch src/server.js",
    "check": "node scripts/check.js",
    "test": "npm run check"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "helmet": "^8.1.0"
  }
}
